doctype html 
html
    head
        title Natours #{tour}     //- without = it works
        link(rel='stylesheet' href='css/style.css')
        link(rel='shortcut icon' type='image/png' href='img/favicon.png')
    body
        h1= tour
        h2= user.toUpperCase()
        //- h1 The Park Camper  - is for making comment not shown in html as comment   

        - const x = 9
        h2= 2*x

        p This is just some text